import numpy as np
import math as m

# full_vec_length = 5
# emptyLists = [[] for _ in range(full_vec_length)]
# # Ctheta_spans = np.array(emptyLists, dtype=object)

# r_hub_vec_full = np.ones(full_vec_length)
# emptyLists[0] = r_hub_vec_full

asdf = np.linspace(10,20,30)
qwer = 2*asdf
# print(asdf + qwer)


r_mean = 55
z_1m = 30

b=3
r = np.linspace(50,60,51)

print(r)

# asdf = np.sqrt(r)
# qwer = 1 + 2*(b*r_mean/z_1m)**2 * (1-(r**2)/(r_mean**2))
# ttt=z_1m * np.sqrt(qwer)      # [8.133]